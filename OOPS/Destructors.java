public class Destructors {
  

  // constructors creates object
  // Destrcutors destroys objects
  // destructors we dont use in java because garbage collector do for us 
  // garbage collector(automatically delete which r not using)
}
